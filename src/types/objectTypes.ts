// array
const array: number[][] = [[1, 2, 3, 4]];
const numArr: number[] = [1, 2, 3];

// enum
enum ROLE {
	// SUPER_ADMIN,
	ADMIN,
	STAFF,
}

// const superAdmin: ROLE = ROLE.SUPER_ADMIN;
const admin: ROLE = ROLE.ADMIN;
const staff: ROLE = ROLE.STAFF;

// tuples
const tuples: [string, string, number] = ['dang tien hung', 'nam dinh', 20];
